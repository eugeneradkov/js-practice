# 🧠 Git Cheatsheet — базовые команды

## 📍 Инициализация проекта и привязка к GitHub

```bash
git init                             # Инициализирует локальный репозиторий
git remote add origin https://github.com/ТВОЙ_НИК/ИМЯ_РЕПО.git
                                     # Привязывает локальный репозиторий к GitHub

echo "# js-practice" > README.md     # Создаёт README с заголовком
git add .                            # Добавляет все файлы в индекс
git commit -m "Initial commit"       # Создаёт коммит с сообщением
git branch -M main                   # Переименовывает ветку в main
git push -u origin main              # Отправляет коммит на GitHub
________________________________________________________________________
git add .                            # Добавляешь все изменения
git commit -m "Краткое описание"     # Создаёшь коммит
git push                             # Отправляешь на GitHub
________________________________________________________________________
git status                           # Показывает текущий статус файлов
git log                              # Список коммитов